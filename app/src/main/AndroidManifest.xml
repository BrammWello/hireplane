<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TicketStatusActivity"></activity>
        <activity android:name=".SeatSelectionActivity" />
        <activity android:name=".TicketSuccessActivity" />
        <activity android:name=".MakePaymentActivity" />
        <activity android:name=".BookingDetailsActivity">

        </activity>
        <activity android:name=".TicketDetailsActivity" />
        <activity android:name=".AirportDepatureSearchActivity" />
        <activity android:name=".AirlineSelectionActivity" />
        <activity android:name=".DestinationFlightSearchActivity" />
        <activity android:name=".TravelDatePickerActivity" />
        <activity android:name=".ClassTravellerPickerActivity" />
        <activity android:name=".FlightSearchActivity" />
        <activity android:name=".FlightsActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".OnBoardingActivity" />
        <activity android:name=".SplashScreenActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>